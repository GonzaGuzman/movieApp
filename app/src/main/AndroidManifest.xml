<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zalo.movieappchallenge">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".appController.AppController"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_splash_tmdb"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_splash_tmdb"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieAppChallenge"
        tools:targetApi="31">
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".search.searchActivity.SearchActivity"
            android:exported="true" />
        <activity
            android:name=".detail.detailActivity.DetailActivity"
            android:exported="true" />
        <activity
            android:name=".home.homeActivity.HomeActivity"
            android:exported="true" />
    </application>

</manifest>